<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminpanel">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminPanel">
        <activity android:name=".splash_screen"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".student_pack.student_pack"
            android:screenOrientation="portrait"/>

        <activity android:name=".admin_login"
            android:screenOrientation="portrait">

        </activity>
        <activity android:name=".Admin_category_add"
            android:screenOrientation="portrait"/>

        <activity android:name=".users_manage.Admin_admin_add"
            android:screenOrientation="portrait"/>

        <activity android:name=".users_manage.Admin_company_add"
            android:screenOrientation="portrait"/>

        <activity android:name=".users_manage.Admin_student_add"
            android:screenOrientation="portrait"/>

        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>